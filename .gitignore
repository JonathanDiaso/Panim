.DS_Store
_*.html
_*.js

# Python bytecode from tools/ — never content.
__pycache__/
*.pyc

# Masters never go in git. They live on the laptop and the MAS drive (README, "The audio here must stay same-origin").
*.wav
*.aif
*.aiff
*.flac

# The site's audio is served from Cloudflare R2 (content/audio-host.js), never from git.
# These folders stay on the laptop as the upload source; tools/upload-audio.sh.
audio/
